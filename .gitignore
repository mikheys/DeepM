# ── Node ──────────────────────────────────────────────────
node_modules/
dist/

# ── Rust build artifacts ──────────────────────────────────
src-tauri/target/
src-tauri/gen/
src-tauri/.cargo/

# ── llama.cpp binaries ────────────────────────────────────
# Download separately from github.com/ggerganov/llama.cpp/releases
src-tauri/binaries/
# CPU engine subset staged for bundling (generated from binaries/)
src-tauri/engine/
# CUDA add-on pack staged for release
src-tauri/cuda-pack/

# ── OCR resources staged for bundling (stage with scripts/stage-ocr) ──
# Bundled Tesseract (exe + DLLs + tessdata-standard/fast) and default
# RapidOCR PP-OCRv5 cyrillic models (det.onnx/rec.onnx/dict.txt).
src-tauri/tesseract/
src-tauri/rapidocr/

# ── Downloaded archives & partial downloads ───────────────
*.zip
*.crdownload
*.7z
*.tar.gz

# ── Model files (downloaded in-app) ──────────────────────
*.gguf
*.ggml

# ── Environment ───────────────────────────────────────────
.env
.env.local
.env.*.local

# ── OS ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# Build log
build.log
# CUDA slim pack staging
src-tauri/slim-pack/
rapidocr-check.log
etap-1-mvp-yadro-i-model.md
etap-2-sistemnaya-integraciya.md
etap-3-dizayn-i-dopolnitelnye-funkcii.md

# ── Local art sources / scratch (not shipped; icons live in src-tauri/icons) ──
/*.ai
/*.svg
*.tmp
~*